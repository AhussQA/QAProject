/**
 * 
 */
"use strict";
function placeholder(){
	var ptag = document.createElement("p");
	var placeholderString = "This is a placeholder Update later";
	ptag.innerHTML= placeholderString;
	document.body.appendChild(ptag)
}